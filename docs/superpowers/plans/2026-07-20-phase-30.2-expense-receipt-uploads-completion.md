# Phase 30.2 — Expense Receipt Uploads — Completion Report

**Date:** 2026-07-20
**Plan:** `docs/superpowers/plans/2026-07-20-phase-30.2-expense-receipt-uploads.md`
**Status:** Complete, final whole-branch review clean (Ready to merge: Yes), live-verified against real `erp-lfd` data.

## Summary

The second and, per the brief, last planned consumer of the Phase 30 file-attachments foundation. `finance_admin` can attach one or more receipt photos/scans to an expense record; anyone with `accounting.expense.view` can retrieve them. No new capability anywhere, zero changes to the Phase 30/30.1 foundation (`capabilityMap.ts`, both `/api/attachments` routes, `storage.rules`), zero changes to P&L.

Three tasks, all approved clean on first pass, zero fix cycles anywhere in this phase:

1. **`src/lib/expenses/store.ts`** — `queryExpenses` extended with new `ExpenseAttachmentRow`/`ExpenseRow` types, resolving each expense's attachment list at the same call site both `GET /api/expenses` and the expense list page already use — mirroring `getLabRecords.ts`'s Phase 30.1 pattern exactly. `GET /api/expenses` itself needed **zero code changes** — it already forwards `queryExpenses`'s return value directly, so the new field flowed through for free. The raw `Expense` interface (`src/lib/types/expense.ts`) stays untouched; `ExpenseRow extends Expense` is a separate, derived type, keeping `src/lib/reports/pnl.ts:64`'s direct `doc.data() as Expense` cast correct — confirmed by both the task reviewer and the final whole-branch reviewer independently, not just asserted in a comment.
2. **`src/app/(dashboard)/expenses/page.tsx`** — a new "Receipts" column rendering plain download links (`GET /api/attachments/[id]`), handling zero/one/many attachments with one conditional. Every other line in the file (capability guard, branch/recorder-name resolution, empty state, "Record expense" link) confirmed untouched via diff stat (21 insertions, 0 modified lines).
3. **`src/app/(dashboard)/expenses/new/AttachReceiptForm.tsx`** (new) + **`ExpenseForm.tsx`** — a post-create attach-receipts step. `POST /api/attachments` requires the related document to already exist, so the expense is created first (unchanged validation/fields), then the form swaps to an in-page attach step keyed on the new expense's id, with a "Done" button performing the navigation that used to happen immediately. Multiple uploads work via functional state (`setAttachedFileNames`) without unmounting the upload control between uploads.

## Verification

- **`npm run build`**: confirmed clean (zero TypeScript errors) by each implementer per-task, and independently re-run by the controller after all three tasks — clean.
- **`npm test`**: 505/505 passing (+3 over Phase 30.1's 502), independently re-run by the controller after all three tasks.
- **Task-level review**: all three tasks approved clean on first pass, zero Critical/Important findings. A handful of Minor, non-blocking notes across all three (N+1 attachment query — plan-mandated, matches the accepted `getLabRecords.ts` precedent; no explicit branch-scoping test — structurally guaranteed by query composition; a hypothetical non-JSON error response edge case in the upload form — not reachable given the real route always returns JSON; a pre-existing, unrelated "middleware deprecated" build warning).
- **Final whole-branch review (Opus)**: Ready to merge = Yes, zero Critical/Important findings. Independently re-verified all three cross-task risks named in the dispatch: `pnl.ts:64` confirmed still casting raw docs and never importing `queryExpenses` (the `ExpenseRow`/`Expense` split is genuinely load-bearing, not just commented); `ExpenseAttachmentRow` field names confirmed consistent end-to-end from Task 1 through Task 2's consumption; `capabilityMap.ts`/both attachments routes/`storage.rules` confirmed genuinely absent from the diff. Four Minor notes (attachment-query scaling, missing `aria-label` on the file input, an array-index React key on an append-only list, an `Attachment` cast assuming `createdAt` is always present — consistent with the foundation's own existing assumption) — none requested a fix.

### Live verification against real `erp-lfd` data

Browser automation was attempted first (per this project's standing practice) but blocked by a Claude-in-Chrome site-permission denial for `localhost:3000` that persisted even after the user granted it in Chrome — the same recurring failure class this project has hit in Phases 21/22/23/26/26.1. Per the user's explicit choice, substituted the HTTP-level fallback those phases used.

A genuine, separate infrastructure issue surfaced mid-verification: the pre-existing dev server on port 3000 was stale/unresponsive (hung indefinitely on a plain `GET /login`) — the same "stale dev server" failure class Phase 19.2 diagnosed. Worked around by starting a fresh `next dev` instance on port 3010 (confirmed healthy — 200 on `/login` after a normal cold Turbopack compile) and running verification against that instance instead.

Real session cookies were minted via Admin-SDK custom-token exchange (the established pattern from Phases 18/26/26.1/29) for the real `finance_admin` (`finance.admin@lfdservices.com`) and `general_manager` (`test.gm@lfdservices.com`) accounts already in `erp-lfd`. Results, all against real production data:

1. `finance_admin` created a real expense — `201`, real id `gekeqOpf9Ns48Ru4odmt`.
2. `finance_admin`'s real receipt-upload attempt correctly returned the Phase 30.1 wrapped `502` / `"Could not upload the file — try again"` — the real Storage bucket for `erp-lfd` remains unprovisioned (Phase 30's known, unchanged outstanding item) — this proves Task 3's wiring genuinely reaches the real foundation route, not just the emulator.
3. `general_manager` (view-only) immediately saw the new expense via a real `GET /api/expenses`, with `attachments: []` correctly reflecting the failed upload — proves Task 1's `queryExpenses` extension works against real Firestore, not just the emulator.
4. `general_manager` correctly blocked `403` from creating an expense.
5. `general_manager` correctly blocked `403` from uploading an attachment.

Per the user's explicit approval of this exact plan, the real test expense (`gekeqOpf9Ns48Ru4odmt`) was left in place rather than deleted — matching Phase 13's explicit-decision precedent for live-verification data, not a default. Two ad-hoc verification scripts were created for this check and deleted immediately afterward — never committed (confirmed via `git status`).

## Outstanding items (unchanged by this phase, named rather than silently carried)

- **The real Storage bucket for `erp-lfd` still isn't provisioned** — both this phase's and Phase 30.1's uploads correctly fail with a clear `502` rather than a crash, but neither can complete a real upload against production until this is done in the Firebase console. This was Phase 30's headline outstanding item and remains so.
- **This is the last planned phase in the file-storage-foundation sequence** — both applications (lab scans, Phase 30.1; expense receipts, Phase 30.2) are now code-complete and correctly wired, blocked on the same infrastructure step.

## Correction made to CLAUDE.md during this phase's wrap-up

CLAUDE.md's own text claimed "Phase 30.1 is not yet tagged" — checked directly against `git tag`, this was stale: `phase-30.1-baseline` already exists, pointing at commit `4e030b4` (the commit immediately before this phase's Task 1). Flagged and corrected rather than silently carried forward, per this project's established CLAUDE.md-staleness practice.
