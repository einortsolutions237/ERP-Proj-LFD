# Phase 13.1 — Medical Secretary Role — Completion Report

Plan: `docs/superpowers/plans/2026-07-05-phase-13.1-medical-secretary-role.md`
(approved as proposed, single task, Opus review, no separate final
whole-branch review per the plan's stated reasoning — one file, one
commit).

## Carried-over fix from the Phase 13 audit

`customers/[id]/page.tsx`'s `notes` gating was already fixed in the
previous session (commit `dacb028`, live-verified). Confirmed still in
place at the start of this phase. No action taken — not this phase's work.

## Commit

| Task | Commit | Files | Lines |
|---|---|---|---|
| Add `medical_secretary` role | `4c86940` | 1 | +12 / -4 |

Base: `dacb028`. Head: `4c86940`.

## What shipped

`medical_secretary` added to `ROLES`. Two existing shared role-group
constants (`CASHIER_BRANCH_MGR`, `CLINICAL_ROLES`) were split rather than
extended: new `CRM_VIEW_ROLES`/`CLINICAL_VIEW_ROLES` constants carry the
new role for `crm.customer.view`/`clinical.record.view` specifically,
while `pos.sale.create`, `pos.sale.view`, `crm.customer.create`, and
`clinical.record.create` keep their original constants, unchanged —
confirmed byte-identical role arrays for all four, both by the task
reviewer and independently re-derived, not trusted from the report. Two
capabilities reserved for later phases (`clinical.appointments.manage`,
`seminars.attendance.view`), both granted to nobody (`[]`). No other
file needed to change — every downstream behavior (customer detail page
gating, staff-creation role dropdown, roles/permissions matrix) was
already capability-driven from Phase 13.

## Verification

Opus task review independently walked the full `ROLE_CAPABILITIES`
object and confirmed `medical_secretary` resolves to exactly 4
capabilities (`clinical.record.view`, `crm.customer.view`,
`hr.leave.request`, `hr.attendance.self`) and nowhere else. Zero
Critical/Important/Minor findings.

Live verification (real data, per this project's standing policy):
created a real `medical_secretary` staff account via `/staff/new` — role
dropdown picked it up with zero UI changes, same as `doctor` in Phase
13. Signed in as that account and confirmed on the existing "Test
Patient" customer page: sees Purchase history (empty), Notes, and
Clinical record (including the Phase 13 treatment) — all correct, since
`crm.customer.view`/`clinical.record.view` are both held. **No "Add
treatment" button rendered** — confirms `clinical.record.create` is
correctly absent. No Edit/Delete controls — confirms `crm.customer.manage`
is correctly absent. A direct `POST /api/treatments` attempt as this
account returned 403. Session restored to the original `super_admin`
afterward; all temporary scripts and ID tokens deleted, never committed.

`api/customers/[id]/route.ts` was never opened this phase — confirmed
by `git diff --stat` showing only `permissions.ts` in the diff. TD-3
remains open, re-flagged, not fixed, per the plan's explicit constraint.

## Assessment

**Ready to merge: Yes.** Zero findings at any severity. All exit
criteria confirmed both by independent code review and live behavior
against the real running app.
