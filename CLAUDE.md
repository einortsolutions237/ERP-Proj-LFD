# LFD Services — Enterprise ERP

## What
Enterprise ERP for LFD Services (starting as a single location, architected for multi-branch growth). Supersedes the earlier "MediCore ERP" healthcare-domain scope — this is a business/retail-and-services enterprise design spanning POS, inventory, CRM, accounting, and HR.

Stack: Next.js (App Router) + TypeScript + Tailwind CSS, Firebase Auth + Firestore + Cloud Functions, deployed on Vercel. Firebase project: `erp-lfd` (migrated from `lfd-erp-4713b` on 2026-07-03 — the old project had an unspecified account-level problem; all Firestore data and the one real Auth account were copied over preserving doc IDs/UID, and the old project was left untouched as a fallback rather than deleted).

Architecture: modular monolith, not microservices — clean module boundaries inside one Next.js app. Every record carries a `branchId` and every permission check is branch-scoped, starting from Phase 1, even though only one branch exists today.

## Why
LFD Services needs one system to replace fragmented/manual operations — starting with staffing and access control, extending to POS, inventory, CRM, accounting, and HR. Full module vision: `docs/project-brief.md`.

## How

**Permissions** — 6 roles: super_admin (protected, immutable, cannot be demoted or deleted in-app), admin, branch_manager, hr_admin, finance_admin, it_admin. Permissions are mapped per module (POS, inventory, CRM, accounting, HR) — not by role alone — and enforced at three layers: UI, server, Firestore rules, never just one. Firebase custom claims (`role`, `branchId`) are the sole source of truth for authorization; Firestore is profile metadata only and is never read to make an access-control decision.

**Never repeat these — each caused a real production issue in an earlier build of this project:**
- No Proxy-based Firebase SDK initialization — use direct SDK calls
- No hardcoded Firebase credentials or fallback values in client code — env vars only, no `??` fallback to literals
- Don't let tooling silently regenerate/empty `tsconfig.json` — verify it after any AI-assisted scaffold
- No `Math.random()` for temporary passwords or any security-sensitive value — use a CSPRNG
- Firestore rules default-deny; every collection needs an explicit, tested rule — no blanket permissive rules
- Session cookies: don't hardcode `secure: true` + `sameSite: none` — it silently drops cookies in non-HTTPS preview environments
- No public self-registration route — accounts are created by a permitted internal role only
- Cloud Functions v2, not v1 (deprecated)

**Audit log** — every login, failed login, logout, and create/edit/delete on customers, staff, or permissions gets an immutable log entry, written server-side only. Never expose an edit or delete path for audit entries.

**Login has two paths, chosen server-side by account role, not by the client:**
- `super_admin` and `admin` (the roles where a failed-login attempt is the highest-value security signal) verify the password server-side against the Identity Toolkit REST API. Every attempt — success or failure — is written to the audit log by code we control; this path is tamper-proof.
- `branch_manager`, `hr_admin`, `finance_admin`, `it_admin` sign in via the Firebase client SDK directly. A failed attempt never reaches our server on its own — the client reports it best-effort after the fact. Treat these `login_failed` entries as telemetry, not a tamper-proof control; this is an accepted trade-off, not a gap to silently fix later.
- The routing decision (which path a given email uses) is made by a pre-flight server lookup of the account's role before any password is checked — this necessarily reveals whether a given email belongs to a `super_admin`/`admin` account (a narrow account-role signal, not full account-existence enumeration — unknown emails and non-strict-role emails get an identical response). Accepted for an internal-only ERP; revisit if this app ever gets external-facing accounts.

**Catalog & stock ownership** — `products` and `services` are org-wide catalog entries, not branch-scoped (a SKU/price is the same everywhere). What's branch-specific is *quantity on hand*: `productStock` holds one doc per branch+product, and its `quantity` is a derived, server-maintained total — never directly settable. Every stock change writes an immutable `stockMovements` ledger entry (restock/adjustment/waste/transfer_out/transfer_in/sale/void), and the same Admin-SDK write path that creates the movement atomically increments `productStock.quantity` in the same transaction. No edit form ever sets quantity directly.

**POS & sales** — `sales` is branch-scoped (a sale physically happens at one location). Line-item `name`/`unitPrice` are snapshotted server-side at sale time from the current catalog — never trusted from the client and never a live reference back to `products`/`services` (a later price change must not retroactively alter a completed sale). Payment (`cash`/`mtn_momo`/`orange_money`) is a manually-entered record only — the system never calls out to MTN, Orange, or any payment processor to verify it. Selling a product decrements `productStock` and writes a `stockMovements` entry (`type: 'sale'`) via the same transaction discipline as every other stock change; selling a service touches no stock. `cashier` is a new, narrow role (client-SDK login path, `pos.sale.create`/`pos.sale.view` only, zero admin/inventory/supplier access anywhere) — from Phase 5, `cashier` also has `hr.leave.request`/`hr.attendance.self` like every other role, since those aren't module-specific access, they're an employee's own self-service actions.

**Voiding a sale** — full-sale void only; no partial/line-item returns, no payment reversal (the system never verified payment methods in the first place, so there's nothing on that side to undo). Voiding is additive, not destructive: the original `sales` doc's `lineItems`/`payments`/`total` are never touched — only `voidedAt`/`voidedBy`/`voidReason` are set, once, on top, and a second void attempt on an already-voided sale is rejected. Stock reversal is ledger-driven, not re-derived from `lineItems`: for each `type:'sale'` `stockMovements` entry the original sale created, a new `type:'void'` entry (positive delta, same `saleId` link) is written in the same transaction that sets the sale's voided fields — the ledger is the source of truth for what to reverse, not a re-aggregation of the sale's line items. `pos.sale.void` is `branch_manager`/`admin`/`super_admin` only, never `cashier`; a `branch_manager` may only void a sale in their own branch.

**Org-wide entities, and how that principle generalizes** — `products`, `services`, `suppliers`, and (from Phase 4) `customers` are all org-wide catalog/reference entries, not branch-scoped: the same customer, product, or supplier record is the same one everywhere, never duplicated per branch. Each may carry a `*BranchId`-style field (e.g. `registeredBranchId` on a customer, matching the role `supplierId`/similar reference fields play elsewhere) for *reference only* — which branch first touched the record — never for access control or filtering; every role reads the full org-wide set. What's branch-specific is downstream activity tied to a branch (`productStock` quantities, `sales` themselves) — the entity and the branch-scoped activity referencing it are two different things, and only the latter is ever filtered by `branchId`.

**HR — leave & attendance** — `leaveRequests` and `attendanceRecords` are branch-scoped, not org-wide (a leave request or a clock-in belongs to wherever the staff member currently works; `branchId` is copied from the requester's custom claims at request/check-in time — the same claims-are-truth source every other branch check in this app already uses). `hr.leave.request` and `hr.attendance.self` are the first capabilities granted to every role rather than a role-group subset — any staff member requests their own leave and clocks themselves in/out, `cashier` included. Self-approval is blocked by an explicit server-side check (`existing.staffId === user.uid`), not a UI omission — enforced regardless of role, including a `super_admin` reviewing their own request. Approver visibility is a deliberate, new exception to this app's usual branch-scoping: `branch_manager` sees only their own branch's requests/attendance (the same restriction already enforced on `pos.sale.void`), but `hr_admin`/`admin`/`super_admin` see every branch — the first place in the app where a non-`branch_manager` role's reads span branches, because HR review is inherently an org-wide function, unlike a branch-scoped sale. Both collections' Firestore rules are fully closed (`allow read, write: if false`, matching `rateLimits`) rather than getting the open-read treatment `products`/`customers`/`sales` get — the access rule here (own record, OR approver with branch-scoping but never on your own record) is exactly the kind of role logic this project keeps out of `firestore.rules` and enforces server-side instead. "Today" for attendance is the server's **UTC calendar date** — a deliberately documented temporary stand-in (single source of truth: `getTodayDateString()`) until per-branch timezones exist as a real concept in this app; revisit alongside the jurisdiction decision. Approving a leave request also checks it against the requester's other already-`approved` requests for date overlap, inside the same transaction that writes the approval — the same "read-check-write atomic" discipline as the phone-uniqueness and sale-void transactions, just comparing date ranges in application code rather than a Firestore range query.

**Notifications are Cloud-Function-driven, not inline in the routes that cause them** — `api/sales/route.ts`, `api/stock/movements/route.ts`, `api/stock/transfer/route.ts`, and `api/leave-requests/*` have all been through clean, sometimes Opus-tier, audits; adding a notification-creation call inside any of them would mean re-touching an already-verified transaction's diff surface for an unrelated concern. Instead, a Firestore-triggered Cloud Function (v2 API only, per the existing "Never repeat these" rule) reacts to what those routes already write — `stockMovements` creates, `leaveRequests` creates/updates — and creates `notifications` docs from the side. This generalizes past Phase 6: any future side effect of an already-audited write (a report row, a downstream sync, another notification type) is a new trigger reacting to the existing write, not a new line inside the write itself. `notifications` are personal, not role/branch-scoped data — access is ownership-only (`recipientUid == request.auth.uid`), so unlike every other module, there's no `Capability` entry for them at all; a bare authenticated check is the only gate, enforced at the API layer, with Firestore rules fully closed the same way as `leaveRequests`/`attendanceRecords`. Cloud Functions run against this project's explicitly-named `"default"` Firestore database, not the SDK's implicit `"(default)"` — see `src/lib/firebase/admin.ts`'s comment on this; a v2 Firestore trigger must pass `database: 'default'` in its options or it silently never fires. Notification docs get a deterministic ID (`${type}_${eventId}_${recipientUid}`) and are written with `create()`, never `set()` — Cloud Functions triggers are at-least-once, not exactly-once, so a retry of the same event must fail loudly on the already-existing doc (swallowed as a known-harmless case) rather than silently re-write it.

**Reporting reads on demand — no new collections, no materialized aggregates** — Phase 7's sales/revenue and stock-level reports compute everything at request time from `sales`, `stockMovements`/`productStock`, `products`, and `services`, the same already-audited data every other module already writes. This is the read-only counterpart to Notifications' "react to the write, don't touch the write" principle: reporting doesn't even need a Cloud Function, since there's no write to react to — it's a new query, not a new side effect. A report API route's output is always plain aggregated primitives (numbers, strings, already-summed totals) — never a raw Firestore doc or Timestamp passed through — which sidesteps the Timestamp-leak bug class this project has hit twice by construction, not by discipline. Line-item names/prices come straight off `sales.lineItems`'s own snapshot (never re-joined against current `products`/`services`), matching the existing "a later price change must not retroactively alter a completed sale" rule — a report is not exempt from that just because it's read-only. The low-stock threshold comparison (`quantity <= reorderThreshold`) is defined once (`src/lib/inventory/lowStock.ts`) and reused by both the report and the existing Cloud Function trigger (`functions/src/lowStock.ts`) — but since `functions/` is a separate deployable that can't import from the root `src/` tree (the same boundary that already forced `APPROVER_ROLES` to be duplicated there), the comparison itself is duplicated with a "keep in sync" comment, not literally shared code; only the app-side report and any other app-side reads share the real helper.

**Second-branch validation — the org-wide-vs-branch-locked pattern, and what it caught** — every phase since Phase 1 assumed a role is either branch-locked (`branch_manager`, `cashier` — always restricted to their own `branchId`) or org-wide (every other role — should never be restricted), but until Phase 8 the app had only ever run against one real branch, so several routes had silently baked in "restrict to `user.branchId`" as if every caller were branch-locked. Phase 8 found and fixed six instances of this: `POST /api/staff` and `POST /api/departments` (create paths — now accept an explicit target `branchId` for non-branch-locked roles, defaulting to `user.branchId` when omitted, validated against a real `branches` doc); `GET /api/staff`, `GET /api/departments`, `GET /api/stock`, `PATCH`/`DELETE /api/staff/[staffId]`, and `PATCH`/`DELETE /api/departments/[id]` (read/edit/delete paths — now unfiltered for org-wide roles instead of silently scoping to the caller's own branch). The shared logic lives in one place: `isBranchLocked(role)` / `BRANCH_LOCKED_ROLES` in `src/lib/auth/permissions.ts` — every one of the six fixes calls this same helper rather than re-deriving `role === 'branch_manager' || role === 'cashier'` inline, closing off the drift risk of one route's check silently going stale if `BRANCH_LOCKED_ROLES` ever changes. `POST /api/sales` was deliberately left as `user.branchId`-always, even for org-wide roles — a sale is tied to wherever the cashier/register physically is, not a branch you're assigning, so self-inference is correct there, unlike staff/department creation. `GET /api/sales`'s branch-always filter remains an open, undecided question (touches a protected file, no explicit call made) — not a silently-accepted gap. `stock/movements`, `stock/transfer`, and `leave-requests` create/approve were all already correct before this phase and were not touched. Validated with a real second branch ("LFD Services — Ikeja Branch") created entirely through the app's own routes (not direct Firestore writes) — first real catalog entries, first real cross-branch staff, first real multi-branch sale and leave-request approval in the system; the already-deployed low-stock Cloud Function correctly scoped its notification to the new branch's own `branch_manager`.

**Current status**: Phase 1 (Foundation) complete. Phase 2 (Catalog & Inventory) complete. Phase 3 (POS) complete. Phase 3.1 (Void a Sale) complete. Phase 4 (CRM) complete — audited against all 9 exit criteria, all pass; tagged `phase-4-baseline`. Phase 5 (HR — Leave & Attendance) complete — all 9 build-order tasks implemented and task-reviewed, final whole-branch review found no Critical issues and confirmed the self-approval guarantee holds unconditionally; one Important finding (a missing composite index) and two Minor findings were fixed post-review; tagged `phase-5-baseline`. Phase 6 (In-App Notifications) complete and deployed — all 6 build-order tasks task-reviewed, final whole-branch review clean; deployed to `erp-lfd` (see the project-migration note above) and verified with a live functional test exercising all three triggers end-to-end, including the multi-recipient low-stock batch-write path. That live test caught one real bug review couldn't reach (no Cloud Functions emulator was available during code review): the shared Admin-app helper used `getApp()` (looks up an app named `"[DEFAULT]"` specifically) instead of `getApps()[0]`, which threw on every invocation in the actual Cloud Functions v2 runtime — fixed and redeployed, then reverified live. Phase 7 (Reporting & Analytics) complete — all 7 build-order tasks implemented and task-reviewed clean; final whole-branch review (Opus) confirmed all 8 exit criteria pass (voided-sale handling, branch/payment-method/topSellers correctness, low-stock parity with `functions/src/lowStock.ts`, permission scoping including `finance_admin`'s first real capability, zero diff on all 4 protected route files, TD-1/TD-2 both confirmed untouched) and the existing Phase 4 `(branchId, createdAt)` composite index is confirmed sufficient — no new index needed. One Important finding (CSV currency columns weren't rounded to match on-screen `.toFixed(2)` display, risking float-noise like `19.900000000000002` in a financial export) was fixed post-review; several Minor/cosmetic findings (a stale permissions.ts comment, a no-op reshape, an unenforced ISO-string-format assumption between `buildSalesReport` and its page) were left as-is, consistent with prior phases' handling of Minor findings. `productStock`/`products`/`sales` are all currently empty in `erp-lfd` (no data seeded in this environment yet), so several by-hand verification checks relied on structural code-tracing rather than non-trivial real data; a new project-wide policy was set mid-phase as a result — implementer subagents may not write synthetic/test data to the live `erp-lfd` project without the user's explicit go-ahead each time. Phase 8 (Second Branch: Build & Validate) complete — not a new module, a validation pass: fixed 6 confirmed branch-scoping bugs (staff/departments create+list+edit+delete, stock listing — see the architecture note above) plus a carried-over top-sellers sort-by-quantity fix, all task-reviewed clean (Opus for every access-control-relevant one); final whole-branch review found one Important cross-file consistency finding (staff creation re-deriving the branch-lock check instead of using the shared `isBranchLocked` helper — fixed) and no Critical issues. Validated live against a genuine second branch, created entirely through real app flows per an explicit user decision that this exercise's output should be treated as real, permanent operational data (not disposable test data) if it reached that far — it did: `erp-lfd` now has its first real second branch, first real catalog, and first real cross-branch staff/sale/leave-request. TD-1/TD-2 both confirmed untouched by this phase, still open. Phase 8.1 (Live Verification) complete — no new module, no code changes: re-ran the second-branch exercise fresh and bidirectionally (Phase 8 never had a `branch_manager` at the original branch to test isolation both ways; this phase created one), documented in `docs/superpowers/plans/2026-07-03-phase-8.1-verification.md` with a complete per-route table covering every branch-scoped route including the ones that needed no fix. Every check passed both directions (stock, sales, leave-approval queue isolation; staff/sales-report/inventory-report org-wide aggregation via `super_admin` and `finance_admin`) — no new bug found. `GET /api/sales`'s branch-filter question remains explicitly undecided, carried forward unchanged. Phase 9 (Design System, First Pass: Shell + POS) complete — presentation only, no behavior change, scoped to the nav shell and POS/checkout only (the other ~18 screens wait for a follow-up phase). Design tokens: 6 named colors (ink/paper/marine/brass/mist/slate, semantic success/danger, warning reuses brass) plus one narrowly-scoped 7th (`tender-orange`, used only on the Orange Money chip) added mid-implementation once the approved palette turned out to have no true orange; Space Grotesk (display/label) + IBM Plex Sans (body) + IBM Plex Mono (tabular numerics) replacing the never-actually-used Geist fonts (a hardcoded `body{font-family:Arial...}` had silently overridden them since scaffold). Signature element: the three always-rendered split-tender payment rows (cash/MTN MoMo/Orange Money) restyled as distinct branded cards with hand-drawn glyphs and a "stamped" cue on entry, not consolidated into a single-select — split-tender behavior is unchanged. All 3 build tasks task-reviewed clean (Opus for the checkout task specifically, since "every existing behavior works identically" was the exit criterion the whole phase is judged on); zero behavior drift found by any reviewer. Live-verified directly against the real running app in a real authenticated session (not just diff-argued): product-merge vs. service-no-merge cart behavior, payment-sum/balance-due math, submit-button enable/disable, the tender-chip stamped cue, and the global keyboard focus ring all confirmed working exactly as designed. One real limitation, hit independently by two implementers and the controller: this sandbox's browser-automation `resize_window` doesn't actually change viewport width, so true tablet/mobile breakpoint rendering was verified by Tailwind-class tracing (confirmed correct by reviewers) rather than a live resize — flagged, not silently assumed. Phase 10 (Design Rollout: Inventory & Catalog) complete — presentation only, no behavior change, extending Phase 9's design system (same tokens from `src/app/globals.css`, no new ones invented) to Products, Services, Suppliers, and Stock: list/table, form, and detail/edit/new views for all four. Executed as 4 subagent-driven-development tasks (Products → Services → Suppliers → Stock), each with an independent implementer + task reviewer, plus a final whole-branch review; all clean, zero Critical/Important findings anywhere. Established the app's first data-table and first page-`<h1>` pattern (bordered/divided rows, `slate` uppercase header, `success`/`slate`/`danger` status pills, `font-mono` numeric cells, `font-display text-2xl font-semibold text-ink` headings) in Task 1 (Products); Services and Suppliers were confirmed byte-for-byte class matches by their reviewers, not just "similar." Stock required genuine adaptation rather than mirroring — a different shape (one page, no `/new`/`/[id]` routes, inline expandable Adjust/Transfer forms, a danger/success low-stock pill instead of active/inactive) reviewed on Opus given its transaction-logic-adjacent stakes; the `quantityDelta` sign computation and transfer validation were confirmed to sit entirely outside every diff hunk, the strongest possible evidence of zero behavior change. A pre-existing, deliberately out-of-scope asymmetry was found and left untouched, not silently fixed: `stock/page.tsx` reads `productStock` filtered to `user.branchId` unconditionally regardless of role, unlike `api/stock/route.ts` (fixed for org-wide roles in Phase 8) — the page apparently never calls that route. All 9 protected `api/*/route.ts` files (products/services/suppliers/stock, including movements and transfer) confirmed untouched by diff at every task review and again at the final review. Extension plan sent for review and approved before implementation, and a real completion report was written (`docs/superpowers/plans/2026-07-03-phase-10-design-inventory-catalog-completion.md`, covering diff sizes, route-file confirmation, and a per-screen self-critique) — both explicitly required this phase since Phase 9 skipped the pre-implementation review step and Phase 8 skipped a written completion report. No live/browser verification was performed this phase (unlike Phase 9) — judged unnecessary and flagged as such, since the diff is a mechanical class/JSX-wrapper swap around unchanged logic, verified instead by `tsc --noEmit` (clean on all 4 commits) plus three independent rounds of diff-tracing review. Two Minor, non-blocking cosmetic findings from the final review were left as-is. Remaining ~14 screens (staff, roles, departments, branches, settings, audit log, CRM, HR, reports) still wait for further design-rollout follow-ups. Tagged `phase-9-baseline` on request before this phase began; Phase 10 not yet tagged (tag-on-request-only, per established workflow). Phase 11 (Design Rollout: CRM & Core Admin) complete — presentation only, no behavior change, extending Phase 9/10's design system (same tokens, no new ones) to Customers, Staff, Departments, and Branches: list/form/detail views for all four. Executed as 4 subagent-driven-development tasks (Customers → Departments → Branches → Staff), each with an independent implementer + task reviewer (Sonnet for Customers/Departments/Branches, Opus for Staff given its adjacency to `super_admin` protections and Phase 8 branch-targeting logic), plus a final whole-branch review (Opus); all clean, zero Critical/Important findings anywhere. Customers established this phase's one new pattern — a genuine read-only key-value detail page, the first in the app separate from its edit form (Staff/Departments/Branches all correctly kept their existing combined detail+edit shape, no separate view needed). Two judgment calls were surfaced and resolved before implementation rather than guessed at: `StaffForm.tsx` has no `branchId` field in create mode despite `POST /api/staff` (Phase 8) supporting an explicit target branch for non-branch-locked roles — confirmed as a pre-existing, never-wired-up gap and left untouched by explicit decision, not fixed as part of this restyle-only phase. Separately, this phase's research found the same list-page-bypasses-its-own-API-route asymmetry Phase 10 had flagged for `stock/page.tsx` also exists in `staff/page.tsx` and `departments/page.tsx` (both read Firestore directly with an unconditional branch filter, never inheriting their sibling routes' Phase 8 org-wide fix) — now a documented 3-for-3 pattern, flagged together, none fixed here. All 8 protected `api/*/route.ts` files (customers/staff/departments/branches, each with its `[id]` sibling) confirmed untouched by diff at every task review and again at the final review. Extension plan sent for review and approved before implementation, including explicit go-ahead on both judgment calls above; completion report written (`docs/superpowers/plans/2026-07-04-phase-11-design-crm-core-admin-completion.md`), covering commit hashes, diff sizes, route-file confirmation, cross-task class-string consistency (verified by extraction, not eyeballing), and a per-screen self-critique with Staff's entry given the most weight. No live/browser verification performed this phase, for the same reason as Phase 10 (mechanical className/JSX-wrapper swap around unchanged logic) — verified instead via `tsc --noEmit` (clean on all 4 commits) plus two independent rounds of diff-tracing review. Remaining ~10 screens (roles, settings, audit log, HR, reports) still wait for further design-rollout follow-ups. Tagged `phase-11-baseline` on request. Phase 12 (Branch-Scoping Read Fix) complete — a behavior fix, not a design phase, the inverse of Phases 9-11: `staff/page.tsx`, `departments/page.tsx`, and `stock/page.tsx` (all previously flagged as pre-existing, out-of-scope asymmetries in Phases 10-11) each read their collection directly from Firestore with a hardcoded, unconditional `branchId` filter, bypassing their sibling API routes' Phase 8 `isBranchLocked()` scoping. A full sweep of every `page.tsx` under `(dashboard)` (not just the 3 known instances) found a 4th, previously unflagged instance — `roles/page.tsx`, same anti-pattern, same `ADMIN_HR`-only capability gate — and confirmed everything else checked out: `pos/page.tsx` and `customers/[id]/page.tsx` are correctly branch-locked/branch-scoped by design; `staff/[staffId]/page.tsx`/`departments/[id]/page.tsx` are unrelated privacy-check 404s; `attendance/page.tsx`/`leave/review/page.tsx` already correctly role-gate; `pos/sales/page.tsx` textually resembles the bug but mirrors `GET /api/sales`'s own still-never-fixed, explicitly-undecided filter — left untouched, not silently decided by this phase. All four confirmed instances now use the identical `isBranchLocked(user.role) ? ...branch-filtered... : ...unfiltered...` ternary their sibling routes already established in Phase 8. Fixing `stock/page.tsx`'s query alone would have newly exposed a related, previously-masked bug: `StockTable.tsx`'s Adjust/Transfer forms passed one ambient page-level `branchId` to every row regardless of that row's real branch, harmless only because the old bug never showed cross-branch rows to begin with — per an explicit user decision, this was fixed in the same phase by threading each row's own `branchId` (from its `productStock` doc) through `StockRow` → `StockTable` → the forms, which also incidentally fixed a transfer-destination edge case (an admin can now correctly transfer stock into their own branch from a foreign-branch row, previously wrongly excluded). Executed as 2 subagent-driven-development tasks, both Opus-reviewed given the access-control stakes, plus a final Opus whole-branch review — all clean, zero Critical/Important findings. All 5 protected `api/*/route.ts` files (staff/departments/stock, incl. movements/transfer) confirmed untouched by diff at every review. Live-verified against real Phase 8/8.1 accounts (no new test data, custom-token sign-in technique, session restored afterward): `super_admin` now correctly sees both real branches' data on staff/roles/stock; both branch_managers (Ikeja, Downtown) are still correctly blocked from staff/roles entirely and each see only their own branch's stock — their two 2-row sets are exactly the disjoint halves of the super_admin's 4-row set, no overlap, no gap. The one write-action verification (an actual cross-branch Adjust/Transfer) was offered and explicitly declined by the user as unnecessary given the Opus reviewer's hand-traced code-path confirmation. Two Minor, pre-existing, out-of-scope findings from the final review were left as-is: `stock/page.tsx`'s low-stock comparison still uses `<` rather than the documented `<=` parity rule, and `roles/page.tsx` hasn't been through the design rollout yet. Completion report: `docs/superpowers/plans/2026-07-04-phase-12-branch-scoping-read-fix-completion.md`. Phase 12 not yet tagged (tag-on-request-only, per established workflow).

Open decisions not yet locked — jurisdiction/tax compliance target, payment processor(s) for POS beyond manual cash/mobile-money recording, current branch count, accounting integration approach, partial/line-item sale returns (deliberately deferred past 3.1), loyalty/marketing/segmentation/duplicate-merge features on customers (deliberately deferred past Phase 4). Don't assume answers to these; flag if a task depends on one.
